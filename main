import random
import CharaterCreature
import DND

def DiceRoller(num, MaxValue):
    d =0
    MaxValue = MaxValue + 1
    Overall = 0
    while d != num:
        Overall = random.randrange(1, MaxValue, 1)
        d = d + 1
    return Overall

print(CharaterCreature.hamdert)