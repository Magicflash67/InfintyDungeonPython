import random
def DiceRollerTest(num, MaxValue):
    d =0
    MaxValue = MaxValue + 1
    Overall = 0
    while x != num:
        Overall = random.randrange(1, MaxValue, 1)
        x = x + 1
    return "Egg"
print("choose ")